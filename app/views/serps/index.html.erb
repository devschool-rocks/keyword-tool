<%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>

<%= line_chart Ranking.group(:created_at, :id).
                       joins(:keyword).
                       joins(:domain).
                       where("keywords.value = 'coding bootcamp' AND domains.value = 'devschool.rocks'") %>
